import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LoadingPost = () => {
  return (
    <View>
      <Text>LoadingPost</Text>
    </View>
  );
};

export default LoadingPost;

const styles = StyleSheet.create({});
