import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const UserPosts = () => {
  return (
    <View>
      <Text style={{color: 'white'}}>UserPosts</Text>
    </View>
  );
};

export default UserPosts;

const styles = StyleSheet.create({});
